# ClothMatics Website Engineering Reference

### Dashboard partial-load recovery (2026-09-15, deployed)
- Follow-up root cause: saved outfits created before Kaggle full-look support have no `outfitRender`. The initial presentation helper dereferenced that null value after Firestore loading succeeded, leaving the dashboard blank behind the generic load error. `full-look.mjs` now normalizes null, missing, legacy string and current object render values while continuing to allow only `blob:` previews and HTTPS saved images; `app.js` also guards malformed saved-outfit containers. A regression executes all of these cases.
- `app.js` keeps the owner-scoped wardrobe query as the required dashboard read. Saved outfits, wear history, challenge history, style history, today’s outfit, style profile and notifications now fail independently with empty fallbacks, so an unavailable secondary collection no longer discards an otherwise valid wardrobe. Permission-denied failures on the wardrobe itself show a sign-out/sign-in recovery message; email-verification failures retain their specific message.
- `index.html` cache-busts the corrected application module. The existing Gemini outfit selection and explicit user-triggered Kaggle full-look action remain unchanged. Validation: 210 Node tests and 16 Python checks passed. Cloudflare Pages production release `e6fe9cf8` is live; exact hashes matched for `index.html`, `app.js`, the full-look and garment modules, and all published v9-v9.4 artifacts. Protected wardrobe routes still return 401 without authentication, and the existing Kaggle Worker remained online. Kaggle was not restarted or modified.

### Separate Stylist generation actions (2026-09-15, deployed)
- The AI Stylist form now exposes two explicit choices. `Generate my outfit` retains the established Gemini-selected wardrobe cards without starting Kaggle. `Generate with Kaggle` runs the same selection first and then requests the user-owned complete mannequin render; the existing result-level Kaggle retry/regenerate action remains available.
- `web-api.mjs` accepts a single JSON object surrounded by model commentary or Markdown fences and performs only bounded trailing-comma repair. Structurally invalid or non-object responses remain rejected. This addresses observed Gemini responses that were semantically valid but not directly parseable by `JSON.parse`.
- Validation: 212 Node tests and 16 Python checks passed. Cloudflare Pages production release `ce60b3cd` is live, with exact live matches for `index.html`, `app.js`, `web-api.mjs`, `companion.css`, full-look/garment modules and v9-v9.4 artifacts. Protected generation routes remain authenticated and the existing Kaggle Worker remains online; Kaggle was not restarted.

### User-triggered complete outfit mannequin studio (2026-09-14, deployed)
- `src/volume` remains absent. Narrow files: `app.js`, `companion.css`, `full-look.mjs`, `web-core.mjs`, `functions/api/wardrobe/full-look.js`, `_headers`, `index.html`, `cloudflare/ghost/index.mjs`, `kaggle/full_look_request_handler.py`, `kaggle/request_handler.py`, `kaggle/notebook_runner.py`, `kaggle/build_notebook.py`, `deployment/prepare.mjs`, `kaggle/README.md`, and the focused full-look/Kaggle tests.
- Gemini outfit selection retains the established item-card result and never starts Kaggle automatically. The best-result action `Generate outfit on Kaggle` explicitly requests one static full-body mannequin image assembled from the exact selected wardrobe references. Generation progress and the verified result appear below the unchanged item cards; failures keep the Gemini outfit usable and allow a button retry without rerunning outfit selection.
- The selection contract supports two to six items. Separates require a top and bottom; dresses, sarees, lehengas and traditional sets use a one-piece `hero` slot; layers, paired footwear and accessories remain optional. Presentation can be masculine, feminine or neutral and does not override garment construction.
- The authenticated Pages route resolves every wardrobe image by owner ID, rejects private, unavailable and unsafe-host sources, sends no Firebase identity to the GPU, bounds source and output bytes, and forwards ordered multi-reference evidence to the permanent Worker `/full-look` path. Browser-side Gemini comparison requires every selected piece to be present with matching color, pattern and construction; one bounded correction attempt is allowed.
- Kaggle v9.4 adds only the isolated `/full-look` handler. It reuses the already loaded pinned FLUX.2 Klein pipeline, four-step inference, shared GPU lock/cache and source references at 832x1088. Existing `/generate` and selfie `/outfit` code paths are unchanged. The direct-run compatibility artifact SHA-256 is `43475a47512f2ab41de2d18f63671398fd78af6f2583220ae9ce33dd179fb01c`.
- Successful renders are uploaded through the existing owner-scoped upload route and saved as `outfitRender` on the saved outfit. Today’s Pick, saved-outfit cards and saved-outfit dialogs prefer this image while retaining the exact selected-item cards for review. The established Gemini stylist prompt and selection behavior are unchanged; the separate Kaggle action composes whichever grounded pieces Gemini selected, including footwear and accessories when present. Runtime blobs and object URLs are stripped from Core API payloads.
- Validation: 208 Node tests and 16 Python prompt/notebook checks passed. Desktop 1280x800 and mobile 390x844 preview checks had no horizontal overflow and kept all primary actions visible. Cloudflare Pages release `9cb96697` and Ghost Worker version `e45019f0-0a54-4ec3-9b3f-bc283644c548` are deployed. Exact live hashes matched for the app, full-look module, supporting garment modules and every v9-v9.4 artifact; `/api/kaggle-source` matched v9.4; all three Pages wardrobe generation routes returned 401 without authentication; Worker registration remained protected. Worker health reported the shared `9.2.0-multicolor` renderer ready, and an empty `/full-look` request returned FastAPI 422 with the new route's required v1 fields, confirming the v9.4 handler is connected. Kaggle was not started by the agent.

### Direct Kaggle worn-outfit routing (2026-09-13, deployed; Kaggle restart pending)
- `src/volume` remains absent. Narrow files: `app.js`, `ghost-mannequin.mjs`, `functions/api/wardrobe/outfit-ghost.js`, `cloudflare/ghost/index.mjs`, `kaggle/outfit_parser.py`, `kaggle/outfit_request_handler.py`, `kaggle/request_handler.py`, `kaggle/notebook_runner.py`, `kaggle/build_notebook.py`, `deployment/prepare.mjs`, and the focused outfit/Kaggle tests.
- Auto Extract with 3D selected sends one normalized full photo plus bounded Gemini item boxes and versioned appearance manifests to the authenticated Pages route. Pages forwards no Firebase identity to the new Kaggle `/outfit` endpoint. Worn Auto Extract no longer invokes Oracle or browser extraction. With 3D off, the existing Oracle/browser path remains. Explicit flat-lay/hanging Auto Extract and Single Garment retain the established v9.2 `/generate` path.
- The `/outfit` route lazily loads pinned `mattmdjaga/segformer_b2_clothes` revision `584abc1e1d260e23c0fc627c5217a09b2b461046` on CPU. It requires clothing plus human-body evidence before accepting a photo, makes source-pixel RGBA masks for upper clothes, skirt, pants, dress and paired footwear, and never renders footwear. Gemini boxes guide component ownership but do not define the alpha outline.
- Supported clothing cutouts are rendered sequentially through the unchanged `render_request` function. Existing FLUX revision, GPU allocation, NF4/FP16 configuration, FP32 tiled VAE, four steps, category dimensions, prompts and `/generate` behavior remain unchanged. Browser-side photo comparison still blocks rejected generations; the semantic source cutout remains available for review.
- Generated v9.3 artifacts add the parser and `/outfit` handler while preserving the v9.2 renderer version. `deployment/prepare.mjs` publishes v9.3 through the compatibility direct-cell endpoints. Current artifact SHA-256: `1b62a0575dc13c272dda95b60427373130de8d56852e6fbb91f8d5bd2916078c`.
- Validation: 187 Node tests and 14 Python prompt/notebook/parser checks passed. Synthetic mask coverage verifies garment-contained occluder preservation, open pant-leg separation and bounded sock-stem trimming. Live visual quality still requires the user-run Kaggle session.
- Production Pages release `242e8068` and Ghost Worker version `81dde8f9-9d0a-44e6-9648-7262bf54cb11` are deployed. Exact live hashes matched for all checked app, intake, Ghost and v9.3 download assets; `/api/kaggle-source` matched the current v9.3 hash; both Ghost endpoints returned 401 without authentication; Worker registration remained protected. Worker health was offline after publication, so the user must start the current published cell.
- Follow-up rejection fix: the human-evidence floor was relaxed while still requiring both body and garment pixels; a mismatched fashion-parser class may fall back only to the dominant semantic clothing class inside the verified Gemini box, never to the box rectangle. The per-photo catch now preserves the actual API/parser error instead of replacing it with a generic brightness message it did not establish. Validation remains 187 Node tests and 11 Python checks.
- Kaggle traceback follow-up: Transformers 5.16 `SegformerImageProcessor.post_process_semantic_segmentation` requires the full `SemanticSegmenterOutput` and reads `.logits` itself. Passing the logits tensor caused the observed `AttributeError: 'Tensor' object has no attribute 'logits'`. The parser now passes `outputs`; the generated-source regression asserts the old call is absent. Pages release `242e8068` publishes this correction.
- Selfie parser v2 preserves photographed arm/hand pixels where they fall inside the inferred top, dress or lower-garment silhouette instead of punching transparent holes. Lower garments also preserve an overlapping shirt hem and belt within the upper 46% of the garment envelope while leaving the crotch and leg separation transparent. This preserves source pixels rather than inventing hidden textile; the unchanged downstream renderer still removes the person from the final 3D image. Footwear masks trim only a bounded narrow sock/ankle stem above each independently detected shoe body. The `/outfit` response reports preserved and trimmed pixel counts for diagnostics; flat-lay `/generate` remains unchanged. Synthetic coverage raises the Python check count to 14.
- Pages release `bb95aa29` publishes parser v2 and source SHA-256 `1b62a0575dc13c272dda95b60427373130de8d56852e6fbb91f8d5bd2916078c`. Read-only production verification matched every checked asset and the protected endpoints retained their expected 401/405 responses. The stable Worker returned `backend_offline`; the user must stop the old Kaggle cell and run the refreshed direct cell before parser v2 becomes active.
- Parser v2 live-log follow-up: Kaggle successfully parsed and generated a jacket and trousers, but Pages rejected the successful response because `functions/api/wardrobe/outfit-ghost.js` still required parser header `1`. The proxy now requires and returns `X-Outfit-Pipeline-Version: 2`; all 187 Node tests pass. This Pages-only correction does not require another Kaggle restart.
- Pages release `eacf5bcf` deploys the parser-header compatibility correction. Production verification matched all checked assets and the Kaggle source; Worker health returned online with backend `9.2.0-multicolor`, contract 2 and `ready: true`.
- Outfit parser v3 replaces small verified arm/hand and overlapping-shirt regions inside a garment envelope with nearest pixels sampled only from the same observed semantic garment mask. Repaired pixels remain opaque, removing both transparent holes and visible hands from pants/top cutouts. Occlusions over 22% of visible garment pixels remain photographed because unseen texture cannot be recovered reliably. The response reports repaired and preserved counts separately. Parser v3 SHA-256 is `65734f473f431b437e4f931c17b595493c95b434c16455d717733c44c7df68df`; 187 Node and 15 Python checks pass. The Pages proxy requires parser header `3`, so the published Kaggle cell must be restarted once.
- Pages release `8583f5ff` publishes the parser-v3 handshake and refreshed source. Exact production asset/source verification passed; Worker transport remains online and ready, but its currently running Kaggle process remains parser v2 until the user restarts the cell.
- Jacket fidelity follow-up keeps ordinary shirts on one generation attempt and gives jackets one automatic corrected attempt after photo-comparison rejection. Jacket invariants now forbid mirroring, invented pockets/snaps/panels/logos and any visible neck/chest support, while preserving exact closure sides, sleeve marks, muted color and fabric texture. Verifier issues are routed into their matching manifest fields instead of being truncated into color prose; worn-outfit jacket retry uses the extracted source cutout. Rebuilt source SHA-256 is `c2e8e16aeefd605e70c520de62adac6e52557f664e5f1983b7d837d4959a5e18`; 189 Node and 16 Python checks pass. Kaggle must be restarted for the stronger server-side jacket invariant.
- Pages release `a21e3b89` publishes the jacket retry and refreshed direct-cell source. Production assets and source matched exactly; protected endpoints retained expected authorization behavior. Worker health returned `backend_offline`, so the updated Kaggle cell must be started before testing.

### Multicolor and garment-family update (2026-09-12, local release candidate)
- Request-only logging refinement: `kaggle/notebook_runner.py` no longer prints the 30-second idle heartbeat or polls GPU memory for it. The listener and child-process failure checks remain. `kaggle/request_handler.py` logs request IDs, received/completed/error states, HTTP status, cache/busy events, source palette, generation timing and prompt-budget diagnostics; health probes are silent. No headers, credentials, images or raw prompts are logged. Rebuilt v9.2 artifacts; 8 Python checks and a local middleware smoke check passed. Kaggle was not launched and this refinement was not deployed.
- `src/volume` remains absent. Narrow sources: `garment-appearance.mjs`, `garment-palette.mjs`, `garment-taxonomy.mjs`, `garment-upload.mjs`, `ghost-contract.mjs`, `ghost-mannequin.mjs`, `ghost-studio.mjs`, `functions/api/wardrobe/ghost.js`, and `kaggle/` contract/build/runner files.
- Appearance version 2 and analysis cache v11 retain up to 12 sampled color regions, including embroidery and panels. Hex values still come from source pixels, not Gemini's supplied hex. Intake compares the exact image fingerprint before reusing evidence; a crop triggers fresh analysis.
- Color samples are a separate bounded `manifest.palette` array; long prose cannot truncate that array. Kaggle reserves complete palette entries in its prompt and explicitly refuses evidence that cannot fit the existing context instead of silently dropping colors. This does not establish exact color reproduction or segmentation accuracy.
- Photo verification requires one successful verdict per supplied color-region ID, as well as existing overall checks. Missing, duplicate, uncertain or failed color-region verdicts reject the result. Saved-studio generations retain their manifest for diagnosis.
- Geometry for skirts, leggings, blouses, kurta, sherwani, saree, lehenga, traditional sets, jumpsuits, rompers, cardigan, swimwear, innerwear, scarves, knitwear, tops, robes, draped clothing, sleepwear and clothing sets is shared with Kaggle through builder-generated `garment_shapes.json`. Existing jeans-to-trousers mapping remains; shoes/bags are outside this clothing-render update. Gender is not used to infer garment construction.
- Existing FLUX model revision, 4-step inference, guidance, image preprocessing and no-recolor postprocessing remain the baseline. No global tint, palette averaging, automatic paid regeneration or numerical Delta-E accuracy claim is introduced.
- Palette requests require generator response header `X-Ghost-Palette-Version: 1`. The stable Worker already forwards response headers. Website and new Kaggle runner must be activated together; current live Kaggle has not been restarted by the agent. New runner artifacts use `clothmatics_ghost_v9_2` filenames to retain the earlier v9 download.
- Final validation after all garment-family additions: 152 Node tests and 8 Python checks passed; `.deploy/site` prepared successfully. Live GPU photo-corpus quality is pending user-run Kaggle testing. No production deployment of this release candidate is claimed.

Last verified against code: 2026-09-14

This file is the canonical maintained reference for the separate ClothMatics website workspace. The website and mobile app remain separate projects; do not copy implementation claims between them without verifying the website code.

## Inspection contract

- Start with `src/volume` and only files directly referenced from it when that path exists.
- If `src/volume` is absent, report that fact and inspect only the exact feature files needed for the current request.
- Do not scan unrelated directories or re-index the whole repository for a small task.
- Use Context7 only when current library or API documentation is required.
- After every material website code change, update this file with behavior actually implemented and the narrow set of source files involved.
- Never record credentials, access tokens, Firebase ID tokens, private keys, admin PINs, or production secrets here.

## Cross-platform contract

- Mobile application behavior is documented in `D:/stylemateai/StyleMateAI/AGENTS.md`.
- Shared Cloudflare, Firebase, Oracle, extraction, consent, quota and wardrobe behavior must be verified in both codebases before claiming parity.
- Website deployment and mobile deployment remain independent.

## Verified website areas

### Selfie intake routing (2026-09-13, rollback deployed)
- Narrow files: `outfit-intake.mjs`, `footwear-outline.mjs`, `app.js`, `garment-upload.mjs`, `ghost-mannequin.mjs`, `_headers`, `tests/outfit-intake.test.mjs`, `tests/outfit-loader.test.mjs`, and `deployment/verify-live.mjs`. Existing uncommitted work in other features is retained; no mobile behavior parity is claimed.
- Auto Extract requests `photoContext` through intake cache v1. Worn/unknown photos use full-context Oracle/browser extraction first, followed by per-item generation for supported clothing. Explicit flat-lay/hanging photos retain direct crop generation, and Single Garment retains its existing path. Empty worn/unknown detection does not expose the whole selfie as one garment.
- Shoes/footwear and unsupported accessories are photo-only; stale clothing subtypes cannot route shoes to GPU generation. A pair remains one wardrobe item. High-confidence per-shoe outlines from intake vision can mask source pixels into one transparent pair, including opening holes. Invalid or uncertain footwear outlines retain the existing extraction result.
- Worn clothing is reanalyzed against the extracted source fingerprint before generation. Rejected outputs retain the extracted original for review. An unavailable generator stops further GPU attempts for that photo while preserving all extracted items. Cancellation does not save partial work. Worn batches enter per-item review and saving retains a distinct extracted original alongside `ghostMannequin`. Existing flat-lay direct-save behavior is retained.
- Local supplied-selfie smoke checks returned polo (362x501), shorts (250x192), and shoe pair (323x196). Both the browser and inspected Oracle `portrait_region` implementations use a full-person MODNet matte followed by rectangular garment cropping and ownership checks, so normal skin/contact pixels can remain. This is a known limitation of the restored extraction behavior.
- The generic Gemini garment-polygon experiment from deployment `a0ee543d` was reverted after the supplied live photo produced a coarse body-shaped top polygon and skipped the bottom and shoes. Production deployment `ab288dbf` restores the earlier `d6f2bbd1` extraction behavior; only release-marker comments differ. Exact canonical-file and Kaggle-source verification passed. Website validation: 184 Node tests passed. The stable Worker returned `backend_offline` during the final read-only check, so Kaggle must be running before new 3D requests can complete.

### Production release (2026-09-11)
- Explicitly authorized website and Cloudflare deployment completed through the existing account. Cloudflare Pages production branch `main` release: `5d91e648` (`https://5d91e648.clothmatics.pages.dev`), canonical site `https://clothmatics.pages.dev`. Wrangler confirmed upload of the static assets, `_headers` and Functions bundle. A subsequent live-verification command was rejected by the user, so byte-for-byte live checks and post-deploy health were not completed.
- Ghost Worker `clothmatics-ghost` deployed version `c8824bf9-4dca-4a0a-9e6f-596a5ae1c28f`. The existing deployed source was inspected: it already streamed the complete multipart body and copied response headers. `cloudflare/ghost/index.mjs` retains that forwarding and existing KV binding; registration now requires POST and the sync secret, validates HTTPS Quick Tunnel origins, strips identity/cookie/sync headers upstream and rejects generator redirects. The current target was not changed. The sync value from the user's supplied notebook fallback was configured as a Worker secret without printing it.
- `deployment/prepare.mjs` creates an isolated website publish directory and includes downloadable v9 Python/notebook artifacts under `/downloads/`; private inspection files, tests, internal documents and notebook source fragments are excluded. `deployment/inspect-cloudflare.mjs` and `verify-live.mjs` support scoped release checks. `.deploy-private/` and `.deploy/` are ignored. Website suite: 121 passing Node tests, including four Worker authorization/forwarding regressions; Python checks: 6 passing.
- Cloudflare Pages serves unknown `.py`/`.ipynb` extensions through the homepage fallback, so the published direct-run artifact also has a `.txt` endpoint. The runner must fetch `/downloads/clothmatics_ghost_v9.txt`, verify the same SHA-256, and compile it as Python.
- Follow-up Pages deployment `e0600d82` publishes that text endpoint. Live read-only check returned `200 text/plain`, source length 44,660 bytes and SHA-256 `0a47256e07801c8281b52fb93d1d038e5946ee4b4bfc1576ffe57377ba31c2a7`.
- The v9.1 prompt revision now asks for a true ghost-mannequin product render: hidden neutral support, inner opening depth, garment volume, sidewall shaping, fold gradients, contact shadows and subtle product perspective. Visible human/mannequin/skin/body remains forbidden. Kaggle artifacts must be rebuilt and rerun after this prompt change; the model's rendered 3D quality remains an end-to-end GPU check.
- Follow-up Pages deployment `919822ff` publishes the v9.1 ghost-volume website prompt and rebuilt Kaggle text artifact. Live read-only check returned `200 text/plain` with SHA-256 `19d230e1d9b415952a55500949cf60954873e40a5373059efcfffd6418a7186d`. The currently running Kaggle engine must be stopped and restarted from this artifact; no Kaggle execution was performed here.
- Pages later routed the `.txt` artifact through the homepage fallback again, so the rebuilt source is also published as `downloads/clothmatics_ghost_v9_source.js`; `.js` is reliably served as a static asset while its body remains Python and is compiled by the Kaggle launcher. The previous `.txt` and `.py` links remain for reference.
- Nested `/downloads/*` routes also fell through to the homepage in the production project, so the final direct-run source is duplicated at the root path `/clothmatics_ghost_v9_source.js`, where module assets are served directly. The root artifact has the same verified SHA-256 as the generated Python file.
- The root `.js` artifact also fell through to the homepage, so the final root asset is `/clothmatics_ghost_v9_source.mjs`; this extension matches the site's existing module assets and is used by the direct Kaggle runner. Its body remains Python source and has the generated file's SHA-256.
- Pages also routed generated long root source assets through the homepage, so the final runner source is served by the authenticated-free Pages Function `/api/kaggle-source`. `deployment/prepare.mjs` embeds the already credential-free Python source into that route, returns `text/plain` with no-store, and preserves the generated SHA-256. Static source links remain references only.
- The production Pages project continued serving the source-tree placeholder for the generated route, so `deployment/prepare.mjs` also embeds the credential-free Python source as base64 markers in the already-served `app.js` asset. The direct Kaggle cell extracts and verifies those markers; no new route or file-extension behavior is required.
- Per user instruction, no Kaggle execution, notebook replacement, target registration or restart was performed. The user will run v9 manually with `CLOTHMATICS_SYNC_TOKEN` enabled in Kaggle Secrets. Until that happens, new 3D generation is not established as operational; the last observed backend was v8.3.3.

### Garment appearance and 3D fidelity corrections (2026-09-11, local work)
- `garment-upload.mjs` now uses the dedicated single-garment analyzer for Single Garment, and appends the same physical-evidence instructions to outfit analysis. Both caches use v10; previous v9 descriptions are not reused by these analyzers.
- `garment-appearance.mjs` validates vision-selected fabric sample coordinates and measures median sRGB patches from the actual photograph. `visualProfile` contains the source fingerprint, sampled palette, material uncertainty and lighting notes. Samples are observations under the photographed light, not proof of true textile color or fibre composition.
- `ghost-contract.mjs` replaces generic category defaults and inferred full-length shirt hems with specific supported categories and observed construction. Shorts precede generic Bottom; sweatshirts are not sweatpants. Unknown and unsupported categories fail explicitly. Stale titles/AI prose are not fed back as color evidence. Only complete versioned appearance metadata is reusable.
- `ghost-mannequin.mjs` sends structured manifests and compares generated output to its source through the existing AI gateway before intake save. Failed or unverifiable color, texture, construction, graphics, category or empty-opening checks reject the result. This consumes a shared AI allowance call and is model-based screening, not a guarantee of physical accuracy.
- Live read-only health inspection identified `clothmatics-ghost.chiragsharma376.workers.dev` proxying the v8.3.3 Kaggle engine. This inspection does not establish deployment of local corrections. The attached baseline strips mannequin prohibitions and truncates the prompt tail; backend corrections and validation are tracked below as implemented.
- `ghost-studio.mjs`, `garment-progress.mjs`, `garment-review.mjs`, `garment-studio.css`, `app.js` and `index.html` add the reference-inspired SVG hanger loop, actual operation-stage text, reduced-motion support, a scrolling comparison body with visible action footer, and a native accessible styled delete confirmation. Hidden actions are explicitly hidden even when base button CSS uses flex. Source color/texture fields are populated and editable; upload review also exposes construction details and sampled colors.
- Single Garment now attempts metadata analysis on every plan using the existing allowance; when unavailable it falls back to manual review. Auto Extract skips generation for insufficiently visible pieces, crops each garment, and continues per-item on quality rejection. `visualProfile`, construction and passed comparison are persisted on intake. Selecting the original in review does not label it as a generated image.
- Regeneration retains the previous object until the new Firestore transaction commits. Retry-save retains overwrite state and the candidate. A transaction checks the expected 3D image before replacement/deletion. Intake failure cleans up both newly uploaded images. Generated-object cleanup follows document mutation.
- `functions/api/wardrobe/ghost.js` validates and compiles v2 manifests, forwards only image/evidence/category/seed (no identity token), uses the same seed across transport retries, and rejects an obsolete backend without `X-Ghost-Contract-Version: 2`. `kaggle/prompt_contract.py`, `ghost_server.py`, `request_handler.py`, `notebook_runner.py` and `build_notebook.py` produce a replacement notebook with field-aware token budgeting, supported category validation, nonblocking inference, bounded retry-result cache, and no baked-in sync credential. It retains the provided model revision and distilled four-step settings. GPU/photo quality and deployment still require an actual run.
- Final local validation: 117 Node tests, 6 Python contract/notebook checks and 13 browser regression assertions passed. Repeated metadata normalization retains validated sampling coordinates while discarding model-provided hex values. Browser checks cover measured RGB, review reset, rejected output, safe replacement retry and delete cancellation; 390x844 and 1280x800 layouts keep dialog actions within the viewport without horizontal overflow. These counts include the current workspace suite, not only this feature. The ready-to-import artifact is `kaggle/clothmatics_ghost_v9.ipynb`; rollout and Worker forwarding checks are documented in `kaggle/README.md`. No v9 GPU run or production deployment was performed in this task.

### Lower-garment fidelity and website comparison (2026-09-12)
- The established shirt/top generation path retains its existing portrait dimensions, quality flags and prompt language. Bottoms alone receive category-specific construction fields for waistband/rise, fly/closure, crotch/inseam, leg silhouette, both hems/cuffs and pocket layout through `garment-upload.mjs`, `ghost-contract.mjs`, `garment-taxonomy.mjs` and the Kaggle prompt contract.
- Auto Extract uses 14% crop padding for supported lower garments while keeping the proven 5% path for other categories. The Kaggle runner keeps tops at 768x1024; trousers and other long bottoms use a source-shaped bounded portrait width, while shorts use a bounded near-square 896px frame. Request-only logs now include source and output dimensions.
- Vision color points accept documented high-confidence numeric values, safe role aliases and `[x,y]` correction only when the swapped point fits the garment box. The Pages proxy requires at least one photo-sampled base color before spending GPU work, preventing the observed empty-palette generation path.
- Lower garments receive three additional comparison checks: exactly two legs/openings, matching rise/crotch/inseam and matching leg silhouette. One failed lower-garment comparison triggers one new-seed generation whose bounded color evidence includes the verifier's concrete correction. Tops do not use this automatic retry.
- A twice-rejected candidate remains visible in the 3D Studio for original/output comparison but is never persisted. The browser console records bounded structured checks, issues, category, seed and request id without image data, credentials or raw prompts; the Pages proxy forwards the Kaggle request id.
- `deployment/prepare.mjs` publishes v9.2 through both v9.2 downloads and compatibility/direct-run URLs, including `/api/kaggle-source` and the embedded app marker. It also refreshes those generated files in the repository root so Git-triggered Pages deployments do not replace them with placeholders. No Kaggle process is started by website deployment; the user must rerun the published cell to activate the updated GPU code.
- Validation: 164 Node tests, 9 Python prompt/notebook checks and 16 browser regression assertions passed. The browser regression confirms a lower-garment automatic correction, rejected preview visibility and persistence blocking in addition to prior replacement/delete safety checks.

### Direct 3D intake routing (2026-09-06)
- Supersedes earlier after-save generation notes: `app.js` calls `generateGhostFromPhoto` during selected 3D intake, before review or wardrobe creation. Single Garment uses its normalized photo; Auto Extract uses each detected garment crop (the full photo for a single detected item). Successful generated output is reviewed, then stored alongside an original image in `ghostMannequin`; 3D is selected on the wardrobe card.
- On generation/analysis failure, processing continues through Oracle-first `extractSingleProduction` or `extractRegionsProduction`, with their existing local fallback. The 3D opt-in enables this Oracle attempt; server authorization still applies. No second generation is launched after saving the basic fallback.
- `functions/api/wardrobe/ghost.js` accepts authenticated multipart photo intake with bounded input size, supported MIME/category/prompt checks and the existing bounded generator retry/output validation. Saved-item JSON/GET ownership checks remain. Caller URLs are never accepted and Firebase credentials never go to the generator. Availability is determined by the generation attempt itself, not a separate health probe.
- `ghost-mannequin.mjs` validates/decode-checks intake PNG output before review. Tests: 89 passed, including multipart generation without a Firestore record and credential isolation. Live GPU/photo quality remains an end-to-end check, not established by unit tests.

### 3D Ghost Mannequin for saved garments

- Wardrobe cards and garment details expose **3D Ghost Mannequin**. Once generated, the same wardrobe item shows a **Normal / 3D** image switch on both surfaces; the saved 3D view is selected immediately after generation. The original `image` and its object key stay unchanged, while **Open 3D studio** retains comparison and download access.
- `ghost-ui.mjs` opens an original/generated comparison dialog, runs technical Gemini analysis through the existing authenticated AI gateway and shared allowance, calls generation, uploads the result to the existing owner-scoped R2 upload route, and automatically saves it to the same wardrobe document.
- `ghost-mannequin.mjs` validates Gemini's category and description. Supported categories are shirt, tshirt, trackpants, trousers, cargo, hoodie, jacket, dress and shorts. Unsupported garments receive an explicit message. The source is the existing wardrobe item's `image`, not a potentially multi-person/full-outfit `sourceImageUrl`.
- `GHOST_ANALYSIS_PROMPT` requires Gemini to condition FLUX on the same garment and exact observed fabric, cut, closures, pockets and letter-for-letter graphics. Gemini describes base, secondary, trim and print colors with approximate sRGB hex values when reliable while declaring source garment pixels authoritative. The generated prompt forbids recoloring and white-balance shifts, requires empty neck/sleeve/waist/hem cavities, limits any inner collar to thin garment fabric along its edge, and treats any neck cylinder, stump, support form, torso/body silhouette, head or skin as invalid. The prompt stays below 4,000 characters. `_headers` serves both Ghost modules with `Cache-Control: no-store` so browsers receive prompt updates immediately.
- `buildGhostAnalysisFromItem` maps saved title, category/subcategory/role, primary and secondary colors, material, non-solid pattern, fit, neckline, sleeves and AI description into the same strict FLUX conditioning contract. It also tells the generator to sample the supplied garment pixels directly and preserve hue, saturation, brightness, undertone, print colors and white balance. `ghost-ui.mjs` uses this path immediately when `aiDescription`, `subCategory` or `material` exists, so auto-extracted and analyzed single-garment wardrobe items do not call Gemini again or consume another quota unit. Only legacy/manual items with none of those metadata fields use `analyzeGhostGarment` as a fallback.
- `functions/api/wardrobe/ghost.js` implements authenticated same-origin GET source/download and POST generation. Each request reads the specific Firestore garment and verifies ownership. Stored image sources are limited to HTTPS R2/Firebase/Google Storage hosts. Cloudflare fetches use the supported `redirect: manual` mode and reject any 3xx source or generator response after inspecting its status. No caller-supplied image URL or generator URL is accepted. Firebase tokens are never forwarded to the generator.
- Cloudflare Pages environment variable `GHOST_MANNEQUIN_API_URL` selects the HTTPS generator endpoint. It safely accepts either an origin or the full `/generate` URL, normalizes both to one `/generate` path, and rejects credentials, query strings, fragments and all other paths. Production and the source fallback use `https://clothmatics-ghost.chiragsharma376.workers.dev/generate`; POST sends `image`, `category`, and `prompt`. `wrangler.toml` does not duplicate this dashboard-managed binding because Cloudflare rejects duplicate Pages Function bindings. The public `config.js` records the service location, but generation uses the server environment. No mobile build change is needed.
- A GPU 429 waits according to a bounded retry delay (15 seconds by default, honoring a safe 5-20 second `Retry-After`) and retries at most six times with fresh multipart bodies. This covers the period where a prior timed-out GPU request still owns the single-worker lock. A Cloudflare 524 caused by GPU cold-start receives exactly one delayed server-side restart; any intervening 429 responses use the same bounded wait. A second 524 returns an explicit warm-up timeout message, and a 429 after exhausting the wait returns a clear busy message. Successful responses expose `X-Ghost-Generation-Attempts`. A user retry retains successful Gemini analysis, and retry-save retains the generated blob rather than spending another generation. A 360-second server timeout bounds retrieval, waiting and generation. Closing during processing aborts the browser request; once persistence starts, closing is disabled until saving finishes. Duplicate requests in the same page are suppressed.
- Cloudflare origin statuses 502, 503, 520-523, 525, 526 and 530 are normalized to `backend_offline` with a clear instruction to restart the Kaggle backend connection. This replaces the generic generation failure for an expired Quick Tunnel while keeping the website-only integration boundary.
- Binary output must have PNG MIME/signature, at least 500 bytes, at most 20 MB and decodable dimensions at least 64x64. Original resolution is retained for download and for storage when within the existing 6 MB upload limit. Larger outputs receive a maximum-2400-edge WebP encode before storage.
- Firestore `ghostMannequin` stores `image`, `imageObjectKey`, `sourceImage`, `category`, `prompt`, `kind: ai_generated`, and server `createdAt`. A transaction rejects deleted, foreign, changed-source or already-generated garments; it updates only this additional field. Failed persistence attempts clean up the new upload where possible. Website garment deletion also attempts generated-object cleanup.
- The dialog labels the result as AI-generated and provides download plus original/3D comparison. Saved downloads use the authenticated same-origin route to avoid storage-host CORS dependencies.
- Validation: all 88 Node tests pass. Ghost coverage includes reusable wardrobe metadata, every supported category mapping, strict source-color and empty-cavity FLUX conditioning, saved Normal/3D image selection with an original fallback, analysis fallback, ownership, full-endpoint normalization, multipart fields, token isolation, supported Cloudflare redirect handling, explicit 3xx rejection, delayed 524 recovery, bounded 429 recovery/exhaustion, Cloudflare origin-offline normalization, invalid output, download selection and original-preserving save guards. The current stable Worker reported its replacement GPU target online. A controlled request was accepted and remained in inference past 45 seconds; a second request was also accepted after the first client timeout, confirming that client/proxy timeouts can outlive or release the server lock at different times and require bounded waiting rather than an immediate retry.
- Metadata reuse, strict invisible-support and color conditioning, wardrobe Normal/3D switching, explicit backend-offline handling and bounded busy recovery are deployed through the Cloudflare Pages `main` branch. The configured stable Worker manages the temporary GPU tunnel target, which must be re-registered whenever Kaggle creates a new Quick Tunnel.

### Garment extraction and mobile parity

- Primary files: `garment-upload.mjs`, `production-extraction.mjs`, `browser-extraction.mjs`, `app.js`, `assets/extraction/`, and `functions/api/wardrobe/extract.js`.
- Style Check uses the same full-outfit prompt and generation configuration as mobile `buildOutfitPrompt`/`analyzeOutfit`: width 1024, JPEG 0.8, temperature 0.2, topP 0.9, 8192 output tokens. A UID-scoped v9 SHA-256 image cache reuses analysis for six hours; `fresh:true` requests fresh analysis. Single Garment premium metadata now uses that same prompt and first garment, rather than a separate single-item prompt.
- Each extraction action verifies `users/{uid}.subscription` through `getDocFromServer`. Only `plan: premium` with a future `premiumUntil` enables Oracle. Free, expired and unverified subscriptions use browser extraction. Free Single Garment does not spend an AI call; metadata remains editable. Auto Extract/Style Check still use the shared authenticated AI allowance.
- Browser extraction runs the mobile U2NetP/MODNet models and transpiled mobile postprocessing in a dedicated module worker. The normal worn entry point uses the mobile person-first legacy pipeline, including visibility, fabric recovery, ownership, footwear seams and geometry gates. `mobile/source-manifest.json` records source hashes. `build-mobile-extraction.cjs` regenerates the pure algorithms from the mobile repository; browser decode/PNG output, inference and diagnostics have explicit adapters.
- Verified model weights are split into files under 8 MiB and SHA-256 checked before inference. ONNX Runtime Web 1.22.0 uses WASM and one thread. Assets are same-origin and fetched lazily; no additional background-removal provider is used. Initial single-garment assets are approximately 16 MB; both models plus runtime are approximately 42 MB. Sessions are retained across sequential items and released after two idle minutes. Cancellation terminates the worker; a 180-second local request timeout bounds processing.
- Premium Single Garment tries authenticated Oracle first and then browser extraction on failure. If that still fails, the browser retries a generous 22% Gemini crop where available, then light-fabric preservation. The original selected file remains available for explicit original/cutout review.
- Premium Auto Extract retains successful Oracle regions and fills failed regions through at most one full-context browser batch. Infrastructure failures stop further cloud calls for that photo. Quality rejection stays per region. Hidden/invalid candidates do not independently trigger inference. No cutout is invented when both engines reject it.
- Prepared preview blobs are reused for saving. Upload optimization preserves transparency, caps cutouts at 1000 pixels and originals at 1200, uses WebP 0.88/JPEG 0.82, and applies a second encode at 0.8/0.74 above 1 MB. R2 upload precedes Firestore wardrobe creation; partial saves and cleanup behavior remain in the existing review flow.
- `_headers` permits same-origin module workers and WASM compilation (`wasm-unsafe-eval`), without enabling JavaScript eval or third-party runtime hosts. Preserve the website copy of `assets/extraction/THIRD_PARTY_NOTICES.txt`; mobile notices remain untouched.
- Validation: 71 Node tests passed, including seven behavioral routing/recovery tests. A real Chromium WASM smoke check ran both models on a synthetic shirt; U2NetP returned a 400x397 PNG and MODNet returned one region. This validates browser execution, not photo-corpus quality parity. Browser/Expo image decoding and encoding can differ; authenticated Gemini/Oracle/R2/Firestore end-to-end and real-photo side-by-side comparison remain separate validation steps.
- Cross-repository tests accept `CLOTHMATICS_MOBILE_ROOT`; without it they use the normal sibling `StyleMateAI` directory.

### Website palette and action hierarchy

- `theme-refresh.css` uses the mobile app's canonical palette from `src/constants/colors.ts`: violet `#6C63FF`, pink `#FF4FA3`, indigo `#2D1B69`, lavender surface `#F1EEFF`, and page background `#F8F7FF`. Primary actions use the app's violet-to-pink brand gradient; secondary actions stay white with violet borders. Camera actions, sidebar selection, review cards, dialogs, keyboard focus, disabled controls and mobile review buttons use consistent states. Cutout previews show a subtle lavender transparency checkerboard.
- `index.html` versions the refreshed CSS and app module and uses indigo `#2D1B69` as the browser theme color. Local browser checks at 1280x720 and 390x844 confirmed the exact app color tokens, gradient primary buttons and no horizontal overflow at the mobile breakpoint.
- The app-matched palette is live on the canonical Cloudflare Pages site and was verified from the rendered production page.

### Public affiliate disclosure
- Static public page: affiliate-disclosure.html, canonical route /affiliate-disclosure; no authentication or tracking scripts. Uses the existing legal.css header, typography, contents navigation, and responsive layout.
- Visible tentative network wording names Awin, Admitad, and Optimise Media. Describes commissions, recommendation principles, and optional product fields without claiming approval or universal field availability.
- index.html and affiliate-disclosure.html contain the affiliate-network HTML comment and metadata. The homepage also contains the Admitad Mitgo ownership-verification meta tag (`mitgo-verification`) supplied for the ClothMatics ad space.
- about.html includes a short Shopping & Affiliate Links section linking to the disclosure.
- Public footer links updated in index.html, about.html, privacy.html, terms.html, faq.html, contact.html, photo-guide.html, and affiliate-disclosure.html. legal.css wraps legal footer links; theme-refresh.css keeps homepage policy links visible and wrapping on mobile.
- sitemap.xml lists the disclosure. Existing robots.txt permits public pages; functions/_middleware.js falls through to static assets for ordinary HTML requests. wrangler.toml configures the static root for the clothmatics Pages project.
- Privacy and Terms policy bodies were not changed. Deployment status must be checked separately; local source changes do not establish that production is updated.
- 2026-09-06 refinement: public app surfaces now reuse the admin neutral/lavender tokens (`#f5f3fb`, `#e5e1f2`, `#15122b`) with restrained accent gradients. Wardrobe cards use contained images, stable metadata height, two-line title clamping, and responsive garment detail dialogs in `theme-refresh.css`.
- `garment-upload.mjs` now asks Gemini to distinguish charcoal/graphite/slate/dark grey from true black and returns `colorDetail` plus `fabricTexture`; `app.js` persists those fields and `web-core.mjs` allows safe owner edits. No credentials or provider changes were made.
- 2026-09-06 dialog refinement: `companion.css` gives the saved-garment detail dialog an admin-style two-column header, contained preview, consistent field controls, checkbox cards, responsive one-column mobile layout, and wrapped action buttons so the lower actions remain usable without horizontal clipping.
- 2026-09-06 optional 3D workflow: the Add to Closet review step adds an opt-in `Generate 3D ghost-mannequin image after saving` checkbox. The original/basic wardrobe image is committed first; generation failure leaves it intact and exposes an admin-request action. Existing garments only expose 3D generation when `ghostMannequin.image` is absent, and saved 3D output can be deleted independently through `app.js` and the existing owner-scoped upload route. Pending `ghostGenerationRequests` are surfaced in the admin activity feed.
- 2026-09-06 processing-stage 3D choice: the same opt-in checkbox is inserted into the initial garment upload form immediately before processing, including Auto Extract, so the preference is selected before extraction begins and is carried through to the save step.
- 2026-09-06 camera-screen 3D choice: the Add to Closet camera screen exposes the 3D checkbox beside Auto Extract and Single Garment before either action opens processing; the selected value is passed into the upload flow so generation starts automatically after the original save.
- 2026-09-06 flat-lay fallback and conditioning detail: Auto Extract keeps a clear source photo reviewable when the worn-outfit detector returns no regions, optionally enriches it through the single-garment analyzer, and preserves the selected 3D request. Ghost conditioning now carries visible material texture, color evidence, construction, pockets, closures, graphics, proportions and uncertainty instructions into generation.
- 2026-09-12 Kaggle source deployment: the generated v9.1 ghost-volume runner is published from `.deploy/site` (not the repository root) so `/downloads/clothmatics_ghost_v9.py` serves Python instead of the Pages HTML fallback. The verified production artifact is 45,174 bytes with SHA-256 `19d230e1d9b415952a55500949cf60954873e40a5373059efcfffd6418a7186d`; rerun `deployment/prepare.mjs` and deploy `.deploy/site` when the source changes. The API fallback route is not used for Kaggle downloads.
